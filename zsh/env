# User configuration
# pilot
export PATH="/Applications/Google Chrome.app/Contents/MacOS:$HOME/.pilot/bin:$PATH"
eval $(pilot env)

eval "$(rbenv init -)"
export INTERCOM_USER=patocallaghan

ctags=/usr/local/bin/ctags
echo -e "\n. \"$(brew --prefix asdf)/libexec/asdf.sh\"" >> ~/.bash_profile

